import { MongoMemoryServer } from 'mongodb-memory-server';

let mongoServer: MongoMemoryServer;

// eslint-disable-next-line @typescript-eslint/no-explicit-any
let user: any;

describe('multer service', () => {
    it('should upload a file', async () => {
        // TODO
    });
});
